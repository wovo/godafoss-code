from godafoss import xy, color, colors, image

class python_125_128( gf.image ):
    """
    image generated from ../godafoss-pics/pythons/python_125_128.png
    size 125 * 128
    color depth 8
    """

    def __init__( self ) -> None:
        image.__init__( self, xy( 125, 128 ) )
        self.data = bytes(
        )

    def read( location: xy ) -> color:
        n = xy.x + xy.y * self.size.x
        return self.data
